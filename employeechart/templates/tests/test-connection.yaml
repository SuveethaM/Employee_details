apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "employeechart.fullname" . }}-test-connection"
  labels:
    {{- include "employeechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "employeechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
